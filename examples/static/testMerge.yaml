---
commonOverlays:
  - name: "test"
    query: metadata.labels
    value:
      "app.kubernetes.io/%k-%v": "%v"  # +workload:%k:default="%v":type=string
yamlFiles:
  - path: "../kubernetes/manifests"
